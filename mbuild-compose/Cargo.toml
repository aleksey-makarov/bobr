[package]
name = "mbuild-compose"
version = "0.1.0"
edition = "2024"

[dependencies]
globset = "0.4"
mbuild-core = { path = "../mbuild-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tar = "0.4"

[dev-dependencies]
libc = "0.2"
tempfile = "3.14"
