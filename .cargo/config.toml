[build]
target-dir = "target"

[alias]
build-bundle-launcher = "build -p bobr-bundle-launcher --target x86_64-unknown-linux-musl"
build-sandbox-launcher = "build -p bobr-sandbox-launcher --target x86_64-unknown-linux-musl"
